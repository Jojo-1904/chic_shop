<form method="post" action="{{ path('app_admin_costume_delete', {'id': costume.id}) }}" onsubmit="return confirm('Etes-vous sur de vouloir supprimer ce costume ?');"class="d-inline">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ costume.id) }}">
    <button class="btn btn-danger"><i class="align-middle" data-feather="trash"></i></button>
</form>
